# 2026-06-09

## _coverpage.md 网站开发优化 (Scene#16)

执行了 `_coverpage.md` 的全面优化，共 10 项修改：

### P0 — 安全
- 两个外链按钮 (Zenodo, Radar Archive) 追加 `rel="noopener noreferrer"`，修复反向 Tabnabbing 漏洞

### P1 — 性能与一致性
- 删除冗余的 Google Fonts `@import`（已在 index.html `<link>` 异步加载）
- "31+ Peer-Reviewed" → "34+ Peer-Reviewed"（与 README 对齐）
- 作者单位 "HKUST" → "Independent Researcher"（与 README 对齐）

### P2 — 可访问性与CSS质量
- 新增 `prefers-reduced-motion: reduce` 媒体查询
- 按钮新增 `:focus-visible` 焦点样式
- 删除未使用的 CSS 变量 `--bg2`、`--card`
- `--white` → `--heading`（修正误导性命名）
- 噪点层 `position: fixed` → `absolute`
- 移动端角落元素移除 `right: 20px`，保持居中

### 同步修改
- `book/_sidebar.md`: "29 Peer-Reviewed Sources" → "34 Peer-Reviewed Sources"
